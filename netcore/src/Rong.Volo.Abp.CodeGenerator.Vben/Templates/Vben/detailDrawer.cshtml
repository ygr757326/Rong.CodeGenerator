@using Rong.Volo.Abp.CodeGenerator.Vue.Models.Pages
@inherits Volo.Abp.TextTemplating.Razor.RazorTemplatePageBase<TemplateVuePageDetailDrawerModel>
@{

}
<template>
 <a-drawer v-model:open="visible" title="@Model.EntityInfo.EntityName - 详情" width="40vw">
   <!--  详情页面 -->
   <Detail v-if="visible" :id="id"></Detail>
 </a-drawer>
</template>

<script setup lang="ts">

 import { ref } from 'vue';
 import Detail from './detail.vue';

 const visible = ref<boolean>(false);
 const id = ref<string>('');
 
 /**
  * 打开弹窗
  */
 function open(data) {
  visible.value = true;
  id.value = data.id;
 }

 defineExpose({ open });
</script>

<style lang="less" scoped></style>
