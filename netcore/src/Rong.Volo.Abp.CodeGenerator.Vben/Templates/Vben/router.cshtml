@using Rong.Volo.Abp.CodeGenerator.Vue.Models.Pages
@inherits Volo.Abp.TextTemplating.Razor.RazorTemplatePageBase<TemplateVuePageRouterModel>
@{
}
import type { AppRouteModule } from '@@/router/types';

import { LAYOUT } from '@@/router/constant';

const @{@Model.EntityInfo.EntityCase}Routes: AppRouteModule = {
  path: '/@{@Model.EntityInfo.EntityCase}',
  name: '@{@Model.EntityInfo.Entity}',
  component: LAYOUT,
  redirect: '/@{@Model.EntityInfo.EntityCase}/index',
  meta: {
    hideChildrenInMenu: true,
    icon: '',
    title: '@{@Model.EntityInfo.EntityName}',
    orderNo: 100000,
    policy:'@{@Model.EntityInfo.Permission.Index}',
  },
  children: [
    {
      path: 'index',
      name: '@{@Model.EntityInfo.Entity}Page',
      component: () => import('@@/views/@{@Model.EntityInfo.EntityCase}/index.vue'),
      meta: {
        policy:'@{@Model.EntityInfo.Permission.Index}',
        title:  '@{@Model.EntityInfo.EntityName}',
        icon: '',
        hideMenu: true,
      },
    },
  ],
};

export default @{@Model.EntityInfo.EntityCase}Routes;
