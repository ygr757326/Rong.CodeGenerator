<template>
  <!--  详情页面 -->
  <div class="detail" ref="detailContentRef">
    <a-descriptions bordered :column="props.column" size="small" :labelStyle="width">
      ${schemas.join("\r")}
    </a-descriptions>
  </div>
</template>

<script setup lang="ts">
  import { get${titleCase(folderName)}Get } from '@/api/${firstLowCase(titleCase(folderName))}/${firstLowCase(titleCase(folderName))}';
  import { ref, nextTick } from 'vue';
  import { useI18n } from '@/hooks/web/useI18n';
  import { useLoading } from '@/components/Loading';
  import { GzImagePreview } from '@/components/GzImage';
  const detailContentRef = ref(null);
  const { t } = useI18n();
  const [openWrapLoading, closeWrapLoading] = useLoading({
    target: detailContentRef,
    props: {
      absolute: true,
    },
  });
  const props = defineProps({
    id: { type: String, default: '' },
    column: { type: Object, default: { xxl: 1, xl: 1, lg: 1, md: 1, sm: 1, xs: 1 } },
  });
  // 宽度css属性
  const width = {
    width: '120px',
    'justify-content': 'flex-end',
  };

  let detailData: any = ref({});
  function getData(data) {
    get${titleCase(folderName)}Get(data)
      .then((res) => {
        detailData.value = res;
      })
      .finally(() => {
        closeWrapLoading();
      });
  }
  function init(data) {
    nextTick(() => {
      openWrapLoading();
    });
    detailData.value = {};
    getData({ id: data.id });
  }
  defineExpose({ init });
</script>

<style lang="less" scoped>
  .title {
    margin-top: 20px;
    font-size: 18px;
    font-weight: bold;
  }

  .contentBox {
    min-height: 500px;
    padding: 10px;
    border: 1px solid #ccc;
  }
</style>
